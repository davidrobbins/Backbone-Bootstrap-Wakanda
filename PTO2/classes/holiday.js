//Creating the Holiday classmodel.Holiday = new DataClass("Holidays");//Add Task attributes.model.Holiday.id = new Attribute("storage", "long", "key auto");model.Holiday.title = new Attribute("storage", "string", "btree");model.Holiday.dateHoliday = new Attribute("storage", "date", "btree");model.Holiday.dateString = new Attribute("storage", "string");//Eventsmodel.Holiday.events = {};model.Holiday.events.onSave = function() {//	sessionRef = currentSession(), // Get the session.//	myCurrentUser = currentUser(), // Get the current user.//	myUser = ds.User.find("id = :1", myCurrentUser.ID);	if (this.isNew()) {	this.dateHoliday = new Date(this.dateString);}	}; //end - onSave().