//Run once a day to check if we should add accrued hours to all accounts.var dayInMilliseconds = 1000 * 60 * 60 * 24,	todaysDayNumber = moment().date(),	lastDayOfThisMonth = moment().add('months', 1).date(0).date();function updateAccounts(){		if ((todaysDayNumber === 15)  || (todaysDayNumber === lastDayOfThisMonth)){		//Account accrual time.												/*		new ds.Log({	    	createDate: new Date(), 	        kind: "chron job",	        dataClassName: "User",	        eventName: "save"	    }).save();     	*/    	    		}	      	//    try {	//        // do something	//        tmCount += 1;	//    }	//    catch(e){	//        tmError = 1;	//    }} setInterval(updateAccounts, dayInMilliseconds) //Run every minute  : 60 * 1000 