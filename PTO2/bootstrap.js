//The customLogin() function will now authenticate all users.// We want the listener to be run with the Admin group's privileges.directory.setLoginListener("PTO.customLogin", "Admin");addHttpRequestHandler('/ptoApproval', 'RequestHandlers/handlePTOApprovalRequest.js', 'ptoApproval');//var theChronJobWorker = new SharedWorker("SharedWorkers/chronJob.js", "chronJob")